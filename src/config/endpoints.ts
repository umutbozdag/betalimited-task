const endpoints = {
  CREATE_SESSION: "createsession",
  PRODUCTS: "products",
  SEARCH_PRODUCT: "search",
  ADD_TO_CART: "add-to-cart",
  VIEW_CART: "view-cart",
} as const;

export default endpoints;
